version: 2
sql:
  - engine: "sqlite"
    schema: "schema.sql"
    queries: "query.sql"
    gen:
      go:
        emit_json_tags: true
        json_tags_case_style: "camel"
        emit_interface: true
        package: "db"
        out: "./"
